# 太古界流程梳理与问题记录

更新时间：2026-09-15
文档性质：梳理 + 问题清单，不是运行说明，也不是对现有结果的否定。
对照基线：`优化阶段二/正式主线/砂砾岩地区DFN构造_20260803.md`（脚本级合同）、
`优化阶段二/正式主线/砂砾岩地区正式主线交接文档_20260827.md`（最新状态）。

本文分四部分：砂砾岩方法基线、太古界数据差异、逐步骤思路与合理性、问题清单与改进顺序。

---

## 第零部分：正式链冻结（2026-09-15，P0-1 + P0-2 已处理）

### 0.1 判定依据：版本时间戳

把两个分支的配置与产物按修改时间排列，事实非常清楚——**"合同 v3 + 三属性分支"是更晚的一轮工作**：

| 时间 | 事件 | 分支 |
|---|---|---|
| 2026-09-10 16:10 | 生成三属性归一化合同 **v2**（CurvatureMax 带符号归一化） | v2 |
| 2026-09-10 16:36 | Step5 输出 `taigu_step5_attribute_v2_common_contract` | v2 |
| 2026-09-10 17:02 / 17:32 | Step6A `common_contract_v4` 模型 / 预测体 | v2 |
| 2026-09-10 17:01 → 09-11 11:24 | Step7A v4 → v5_sparse → v6_multiwell_gradient 配置 | v2 |
| **2026-09-14 09:45–09:48** | 生成三属性归一化合同 **v3**（CurvatureMax 先取绝对值） | v3 |
| **2026-09-14 09:52–09:59** | Step5 配置与输出 `taigu_step5_attribute_v3_common_contract` | v3 |
| **2026-09-14 10:00 / 10:34** | Step6A `attribute_v3` 模型 / `attribute_v3_full` 预测体 | v3 |
| **2026-09-14 10:47** | Step7A `attribute_v3_regen` 配置与输出 | v3 |
| 2026-09-14 11:25 / 14:28–14:30 | Step6B v6 → **v7**、Step7B v6 → **v7** | v3 |
| 2026-09-14 17:35 / 09-15 | Step7D `fused_v1`、Step8、Step9 | v3 |

结论：

1. **正式链 = 合同 v3 分支**（09-14 之后），而且 7D→8→9 本来就只消费这条链；
2. v2 分支（合同 v2 + 6A `common_contract_v4` + 7A v4/v5/v6）是 09-10/09-11 的上一轮，
   **没有任何下游引用**；
3. 关键澄清：7A `attribute_v3_regen` 与 7A `v6_multiwell_gradient` 的配置**逐键对比完全相同**，
   只有输入/输出路径和版本名不同——也就是说"多井梯度方位族"这套新方法**已经在正式链里**，
   v6 不是更新的方法，只是同一方法跑在旧合同上。

### 0.2 冻结的正式链

| 步骤 | 配置 | 正式产物 |
|---|---|---|
| 属性合同 | `common/attribute_sampling/configs/taigu_attribute_contract_v3.json` | `output/taigu_attribute_contract_v3/` |
| 空间合同 | `common/attribute_trace_contract`、`common/attribute_demo_grid`、`common/horizon_contract` | 属性主道头、demo 网格、三界面逐道合同 |
| Step5 | `step5_virtual_wells/configs/taigu_step5_attribute_v3.json` | `output/taigu_step5_attribute_v3_common_contract/` |
| Step6A | `taigu_step6a_train_attribute_v3.json` + `taigu_step6a_predict_attribute_v3.json` | `output/taigu_step6a_attribute_v3/`（模型）+ `output/taigu_step6a_attribute_v3_full/`（体） |
| Step6B | `taigu_step6b_medium_v7_anttrack_led.json` | `output/taigu_medium_v7_anttrack_led/` |
| Step6C | `formal_candidate_cheye1_multiscale_density_v1.json`（命名待改） | `output/taigu_step6c_large_v3_attribute_v3/` |
| Step6D | `taigu_step6d_multiscale_v1.json` | `output/taigu_step6d_multiscale_v1/`（当前无下游消费，见 P0-6） |
| Step7A | `taigu_step7a_attribute_v3_regen.json` | `output/taigu_step7a_attribute_v3_regen_full/` |
| Step7B | `taigu_step7b_medium_v7_anttrack_led.json` | `output/taigu_medium_v7_anttrack_led/` |
| Step7C | `taigu_step7c_large_v3_attribute_v3.json` | `output/taigu_step7c_large_v3_attribute_v3/` |
| Step7D | `taigu_step7d_fused_v1.json` | `output/taigu_step7d_fused_v1/` |
| Step8 | `taigu_step8_attribute_multiscale_v1.json`（另有待定稿的 `..._v2_mdjoin`） | `output/taigu_step8_attribute_multiscale_v1/` |
| Step9 | `taigu_step9_multibackground_v2.json` | `output/taigu_step9_multibackground_v2/` |

### 0.3 本次清理动作

旧分支与旧结果已从正式树移出，统一放到：

```text
太古界/_archive_20260915/
  A_contract_v2/            # 合同 v2 分支：common 合同、Step5、Step6A、Step7A v4/v5/v6、Step6B v4/v5
  B_superseded_versions/    # 同一步的旧版本：Step6B v1–v6(+smoke)、Step7B v2/v4/v5/v6
  C_legacy_v1_chain/        # 2026-08-22 v1（OBN 单属性）链残留：Step8 v1 + smoke、各步 v1 配置、旧全链 runner
```

- 共 39 项，约 2.5 GB；移动前已逐个校验"14 份正式链配置均不引用"；
- 移动后复查正式链的 **105 条仓库内依赖全部存在**；
- 归档内容是**移动而非删除**，随时可恢复；确认无误后彻底释放空间：

```bash
rm -rf 太古界/_archive_20260915
```

- 注意：`太古界Step5-9交接文档_20260822.md` 里的复现命令指向已归档的 v1 配置与旧 runner，
  该文档的"复现"章节已失效，需要按 0.2 的正式链回写（列入 P2-8）。

### 0.4 处理决定（2026-09-15）

| 编号 | 决定 | 说明 |
|---|---|---|
| P0-3 | **忽略（作为既定前提）** | 密度口径差异是甲方数据本身的口径；方案适配以"现成的裂缝就是要表达的成像测井井上裂缝"为准，不做口径细分、不做统一标定。后续不再作为缺陷处理，但需在成果说明中保留"口径来自甲方解释成果"的表述。 |
| P0-4 | **当前处理** | 详见 0.5。 |
| P0-5 | **确认要修** | 层位一律以逐道合同为准，禁止各步骤运行时重新计算或填补。 |
| P0-6 | **确认要修** | 根因是 Step7A 实现顺序错误：应消费 Step6D 损伤带上下文，实际却直接用了 Step6A 小尺度背景。 |
| P0-7 | **确认要做** | 需要把层位下伏 50 ms 的补充区域真正预测出来并纳入链路。 |
| P0-8 | **暂缓** | 先争取拿到成像井真实时深；若拿不到，则保留"最近深度覆盖井借用"的时深逻辑（当前实现即为此），不再按缺陷处理。 |

### 0.5 P0-4 修改方案（待实施）

> **状态（2026-09-15 23:15）**：代码已实施并通过模块自检，r2 全链验证已在 tmux 后台启动
> （会话 `taigu_p04_r2`，日志 `太古界/output_p04_r2_logs/`）。落地内容与验收口径见 0.6。

#### 0.5.1 先把范围缩小到真正丢点的地方

对四处回接做了实测核对（2026-09-15）：

| 位置 | 回接对象 | 实测结果 | 结论 |
|---|---|---|---|
| Step8 `correct_taigu_multiscale_dfn_with_well_controls.py` | **Step4 合并裂缝点 → Step2 段** | 容差 0.011 m；埕北310 全部 9 点丢失；405 有 250/385 点落在 0.005–0.010 m 擦边区 | **真正在丢点，本次主修** |
| Step5 `build_taigu_virtual_wells.py` | Step3 成像组 → Step2 段（按 `InputSegmentPath`） | 28,751 行全部精确命中，**MD 距离恒为 0** | 当前无损失，但有"静默丢弃 + 逻辑重复"隐患 |
| Step7A `load_imaging_density_profile` | 同上 | 同上 | 同上 |
| Step9 `build_taigu_multibackground_sections.py` | 同上（另有 `continue` 跳过分支） | 同上；路径不存在时**静默跳过** | 同上 |

原因是两类：① **Step4 合并点没有 X/Y/TIME，必须回接**，而 Step4 合并时按 TVD 邻域取均值，
合并后的 MD 落在两段网格中间（埕北310 两段网格差 0.042 m → 偏离两边各 0.021 m），
比容差 0.011 m 还大；② Step3 的 MD 直接取自段文件，所以距离为 0，容差再紧也不出事，
但四处各写一遍、容差写死在同一数值，任何一处上游变动都会同时静默失效。

#### 0.5.2 改法：抽公共段回接模块

新增 `太古界/common/well_segment_join/`：

- `segment_pool.py`：`list_segment_files()`（优先 `*_seg_*.csv`）、`WellSegmentPool`
  （一口井全部段栈在同一条 MD 轴上，提供 `candidates(md, tol)` 与 `nearest_any(md)`）、
  进程内缓存避免重复读盘；
- `source_segment.py`：`Step4SourceSegmentLookup`（用 Step4
  `all_wells_source_detail_predictions.csv` 把合并点回溯到真正的来源段）；
- `attach.py`：`attach_geometry_by_md(points, pools, tolerance_m, policy, preferred_column,
  geometry_source)` → `(matched, audit)`。

统一规则：

1. **选段优先级**：行内显式来源段 > 来源段字典命中 > |ΔMD| 最小 > 段序（稳定 tie-break）；
2. **容差**：不再用 0.011 m，改用"半采样步长"口径（MD 中位步长 0.125 m → 0.06 m 量级），
   并有硬上限，避免跨到下一个真实采样点；
3. **行序与 ID 不变**：一个输入点只产出一行，不重排、不重新编号；
4. **未命中不再静默丢弃**：写入审计表，含最近段、最近距离、是否超出段 MD 范围、原因；
5. 统一审计字段：`MDJoinStatus / MDJoinReason / MDCandidateCount / MDCandidateSegmentIDs /
   MDCandidateDistancesM / PreferredSegmentIDs / SelectedSegmentID / SelectedSegmentPath /
   MDNearestDifferenceM / InsideSelectedSegmentMDRange / SegmentSelectionPolicy / MDJoinToleranceM`；
6. 统一 summary 片段：输入点数、匹配数、未命中数及原因分布、被选段分布、MD 偏差分位数。

#### 0.5.3 四处接入（只改"取坐标"这一段，不动算法）

| 位置 | 改法 |
|---|---|
| Step8 | 用公共模块替换本地实现（即当前 `..._v2_mdjoin` 的实现抽出去），Step4 点全部可回接并将未命中落审计 |
| Step9 | 换成公共模块；成像点回接审计写入 `section_summary.json` |
| Step7A | `load_imaging_density_profile` 换成公共模块 |
| Step5 | 强监督逐段回接换成公共模块，新增 `step5_strong_supervision_join_audit.csv` |

#### 0.5.4 验证方式

- **单井用例**：埕北310（两段网格差 0.042 m）、埕北古斜405（两段重叠 288 m）、
  埕北313（三段 + 4629–4753 m 空档）、埕北古10（三段完全重叠）；
- **守恒断言**：输入点数 = 匹配数 + 未命中数；无重复行；行序不变；
- **全链差异报告**：改前 / 改后逐项对比 Step5 统一样本行数与正样本数、Step7A 片数、
  Step8 控制点数与井控片、Step9 成像点数量，确认除新增审计外无非预期几何变化；
- 预期：Step5 / Step7A / Step9 结果不变（本来就是精确命中），Step8 的 11 个丢点被找回、
  428 个 Step4 点全部进入审计。

#### 0.5.5 仍需确认

1. 井控片 ID / 微偏移键：沿用原来的"位置编号"（结果与旧版逐字一致，但依赖行序），
   还是改成"井名 + 深度"内容键（与行序解耦，代价是 99 片井控小尺度片一次性位移，
   最大约 31.75 m）？
2. 容差取"半采样步长"（≈0.0625 m）还是固定值？
3. 未命中的控制点只进审计、不参与校正，是否确认？

### 0.6 P0-4 实施记录（2026-09-15）

#### 0.6.1 已确认口径

| 事项 | 决定 |
|---|---|
| 井控片微偏移键 | **方案 B**：改为"井名 + 深度"内容键，接受井控片一次性位移 |
| MD 回接容差 | **按各井半采样步长自动推导**（0.125 m 步长 → 0.0625 m；下限 0.02 m、上限 0.5 m） |
| 未命中的点 | **只进审计、不参与**后续校正（不做就近硬凑） |
| Step5 `SourceSampleID` | 一并改为内容键（井名 + TVD，缺 TVD 回退 MD） |
| 版本命名 | 本轮全部使用 `_r2` 后缀的独立版本目录，不覆盖正式链 |
| 跳过判据 | Step6A 体**逐样点完全一致**时跳过 Step6B/6C/7B/7C |
| Step9 人工确认 | 只人工确认 405 井周两张图，但保留 20 张的生成能力 |

#### 0.6.2 已落地的代码

新增公共模块 `太古界/common/well_segment_join/`：

- `segment_pool.py`：`WellSegmentPool`（一口井全部段栈在一条 MD 轴）+ 进程内缓存
  + `inferred_md_tolerance()` 半采样步长容差；
- `source_segment.py`：`Step4SourceSegmentLookup`（用 Step4 来源明细回溯真实来源段）；
- `attach.py`：`attach_geometry_by_md()` + `summarize_join()` + 统一审计字段；
- `smoke_segment_join.py`：四口多段井自检。

五处回接全部切换到公共模块（删除了本地重复实现）：

| 位置 | 说明 |
|---|---|
| Step5 `build_taigu_virtual_wells.py` | 强监督逐段回接；新增 `step5_strong_supervision_join_audit.csv`；`SourceSampleID` 改内容键 |
| Step7A `load_multiwell_orientation_points` | 旧实现写死 0.05 m 容差且静默丢点；现走公共模块，join QC 写入 summary |
| Step7A `load_imaging_density_profile` | 同上（QC 用途） |
| Step8 `correct_taigu_multiscale_dfn_with_well_controls.py` | 删除本地 340 行重复实现；Step4/Step3 两处回接改用公共模块 |
| Step9 `build_taigu_multibackground_sections.py` | 成像点回接改公共模块；未命中写 `imaging_point_join_unmatched.csv` 并进 summary |

配置口径统一：各步配置删除写死的 `md_merge_tolerance`，改为

```json
"segment_join": {
  "tolerance_m": "auto",
  "policy": "prefer_source_segment",
  "half_step_multiplier": 0.5,
  "tolerance_floor_m": 0.02,
  "tolerance_cap_m": 0.5,
  "max_unmatched_fraction": 0.02
}
```

#### 0.6.3 模块自检结果（2026-09-15）

```text
[用例 A] Step4 合并点 -> Step2 段：4553 点全部匹配，未命中 0，重复 0
         命中真实来源段 4553/4553；MD 距离 中位 0 / p95 0.010 / 最大 0.03125 m
         容差自动推导范围 0.0381–0.0625 m
[用例 B] Step3 成像组 -> Step2 段：1265 点全部匹配，MD 距离恒为 0
[逐井]   埕北310 9/9（旧口径全丢）、405 385/385、313 1210/1210、古10 45/45
```

对照改造前：Step4 有 11 个点被静默丢弃（含埕北310 全部 9 个），405 有 250/385 个点
靠 0.011 m 容差"擦边通过"。改造后全部落入审计，且未命中数恒为 0。

#### 0.6.4 验收方式（已固化为可执行流程）

1. **版本目录**：Step5→Step9 全部使用 `_r2` 后缀的独立版本，正式链不被覆盖；
2. **单井用例**：`python3 太古界/common/well_segment_join/smoke_segment_join.py`
   断言"点数守恒、行序一致、无重复、容差覆盖合并偏移"；
3. **Step6A 体等价性**：`太古界/tools/compare_step6a_volume.py` 逐样点比较新旧 SGY，
   一致则跳过 6B/6C/7B/7C；
4. **全链差异报告**：`太古界/tools/compare_p0_4_runs.py` 输出
   `pass / review_required`，只允许三类预期差异——
   ① 新增审计文件；② Step5/Step5B 的 ID 列；③ Step8 井控片位移
   （位移清单另存 `step8_moved_control_patches.csv`）；
5. **硬校验**：Step8 中/大尺度片位移必须为 0；Step9 status=pass；成像点未命中为 0；
6. **人工确认**：仅 405 井周 XZ/YZ 两张剖面（20 张图仍全部生成）。

一键复现：

```bash
tmux new-session -d -s taigu_p04_r2 'bash 太古界/run_p0_4_r2_chain.sh'
# 日志：太古界/output_p04_r2_logs/（chain.log + 各步 r2 日志 + 对比报告）
```

#### 0.6.5 首轮验证发现与修正（2026-09-16）

首轮 r2 跑到 Step6A 结束，发现两件事，都已修正后重跑：

**发现 1：Step6A 密度体发生了变化（最大差 3.39，1,182 万样点不同）**

- 现象：Step5 的表头统计完全一致（506,471 行、正样本率 0.3502、各 SourceKind 计数相同），
  但 6A 体逐样点比对 `identical=false`。
- 根因：**强监督 5,903 行的行序变了**——改造前是"按 `InputSegmentPath` 分组、段内按 MD"
  输出（`for segment_path, sub in group.groupby("InputSegmentPath")`），改造后变成
  "按组 CSV 原始行序"。行序变化会改变 Step6A 的 `GroupKFold` 分折顺序，
  进而改变模型与预测体（对得上实测：只有这 5,903 行错位）。
- 修正：Step5 与 Step7A 的回接结果统一按 `[InputSegmentPath, MD]` 稳定排序，
  复现改造前的行序。已用 405 井验证：旧序列 5,903/5,903 按序命中，首 5 个 TIME 完全一致。
- 教训：**回接改造必须保持行序**。行序不是显示问题，它会通过分折影响模型和三维体。

**发现 2：6B r2 直接失败于契约守卫**

- 现象：`RuntimeError: Step6A contract mismatch: taigu_step6a_attribute_v3_r2 != taigu_step6a_attribute_v3`。
- 根因：r2 配置从正式配置复制时，`expected_step6a_model_contract_version` 仍指向旧版本名。
- 修正：r2 配置的该键改为 `taigu_step6a_attribute_v3_r2`；同时确认
  `expected_attribute_normalization_contract_version` 仍指向 `taigu_attribute_contract_v3`
  （该键是归一化合同，不是 Step6A 模型合同）。

首轮 Step5 新增审计的实测结果（写入 `taigu_step5_acceptance_summary.json`）：

```text
强监督回接 28,751 行全部匹配，未命中 0，MD 距离中位/p95/最大 全为 0
被选段分布与 Step2 段文件一一对应（桩斜169_seg_001 5394、埕北310_seg_002 5111 …）
```

修正后已在 tmux 会话 `taigu_p04_r2b` 重跑全链。

---

## 第一部分：砂砾岩方法基线（对照用）

砂砾岩正式主线的骨架是"先造合同，再造样本，再造证据，最后几何化和展示"：

1. **公共层**：逐道 T4–T7 层位合同（每道自己的四个层面时间 + 2 ms/10 ms 层窗索引）、
   统一 DFN VTK（`GeometryGroupCode=1` 预测片 / `2` 原始断层三角面）、曲线别名目录。
2. **Step1–Step2**：点挂层位 → 真实井逐点底板。斜井用 MD 做井斜/时深插值自变量；
   只保留 T4–T7 最长连续段；GR + 一组深/浅电阻率必须同一次测井才成立。
3. **Step3**：成像解释重构到同一 DEPT 网格，连续密度 `Density` 是主标签，
   `GT_POINT_FLAG` 只表示真实解释点。
4. **Step4**：2 地层 × 3 曲线对 = 六专家，两阶段（存在概率 × 条件密度）；
   最终每井只留一条合并曲线，优先级 `RD_RS > LLD_LLS > RILD_RILM`，不平均、不补零。
5. **Step5A/5B**：5×5 虚拟井（中心道保留），关键在**按层内相对位置重算虚拟 TIME**；
   属性相似度做标签迁移门控；双标签（`PresenceLabel` 继承、`DensityLabel` 仅正样本）；
   权重 = 置信度/25；按 `SourceWellName` 分组留井验证防止虚拟井泄漏。
6. **Step6A–D**：两阶段连续密度 + 曲率融合（各 0.50）→ 蚂蚁主导的中尺度裂缝带 →
   原始断层硬约束 + 低相干主导的自主断层 → 只做**损伤带上下文**（不产本体几何）。
7. **Step7A–D**：小/中/大尺度分别几何化，原始断层三角面原样携带（哈希校验），
   统一 VTK 双几何组。
8. **Step8**：两级控制源（Step4 常规测井事件 + Step3 成像真值），只动小尺度可修改片；
   成像井周软增强；原始断层与中/大尺度不动。
9. **Step9/9b**：沿井弯曲剖面 + 多背景叠加 + P10/P21/P33 量化。

五条贯穿全链的设计信条：缺失就是缺失（不补零不外推）；不同量级不硬换算；
连续密度/二值存在/真实解释点三种标签分层；预测几何与原始几何全程分组；
每一步留审计与 `status=pass`。

---

## 第二部分：太古界的数据差异（决定哪些地方必须适配）

| # | 维度 | 砂砾岩 | 太古界 | 对流程的影响 |
|---|---|---|---|---|
| D1 | 深度域 | DEPT/MD 与 TVD 基本同轴，时深用于进 TIME | **LAS 深度 = MD，成像解释 = TVD**，两者靠井斜/时深逐行转换 | Step1/2/3 必须显式区分，不能混用 |
| D2 | 成像井时深 | 有本井时深 | **成像井全部没有本井时深**，借用最近常规井（`temporary_neighbor_time_depth=1`） | 成像标签的 TIME 不可信；TIME 域门控/匹配都会受污染 |
| D3 | 层位结构 | T4–T7 四界面，两个层段 | **三界面 Top/Mid/Base** + Base 下 50 ms 延伸窗（`WindowCode=2`） | 层段数、窗定义、验收口径都要改 |
| D4 | 地震网格 | 一套 psdm 网格 + 属性体 | **两套网格**：OBN 2,002,838 道（缺 153,712 道）与属性主网格 2,156,550 道 | 振幅不能作为同网格输入；背景与建模网格要分开 |
| D5 | 建模属性 | Coherence+AntTrack+CurvatureMax+SeisAmp | 只用三属性（振幅退出建模）；蚂蚁 `-1` 语义由"无效"改为"有效低值" | 归一化合同必须统一版本，否则曲率/蚂蚁口径不一致 |
| D6 | 断层 | 已有三角断层面库 | 只有**线段**（8 列、伪 inline/xline=2147483647） | 需线段→三维面→300 m 网格片，参数未在太古界验证 |
| D7 | 井数与监督 | 54 井 / 10 个成像 group | 33 口配置 → **17 口可用 / 8 组 / 6 井**；风化壳强监督仅 2 井 | 模型指标弱、覆盖窄是结构性的 |
| D8 | 测井段 | 一井一主表 | **一井多段**（重叠/缺失），控制点表只有 MD/TVD，无 X/Y/TIME | 下游必须做 MD 回接，且要处理重叠段归并 |
| D9 | 目标范围 | 矿区 25.5×18.3 km + 10 km demo | 当前只做 **5×5 km demo** | 交付范围与砂砾岩不对等 |

---

## 第三部分：太古界当前流程逐步骤梳理与合理性分析

### common 层

**1. 属性主网格与道头合同**（`common/attribute_trace_contract`）
- 做法：三属性体各 2,156,550 道、1 ms、3,001 样；以 CurvatureMax 为唯一空间主网格，
  `TraceIdx` 为统一空间键；AntTrack 与主网格坐标最大偏差 1.414 m，按同一 TraceIdx 读取。
- 合理性：**合理且必要**。砂砾岩也是"一个网格走到底"，太古界两套网格的现实下，
  统一到属性主网格是正确选择；不再逐属性做最近道搜索也避免了三套定位互相矛盾。

**2. 三属性裂缝证据合同**（`common/attribute_sampling`）
- 做法：把属性映射为 0–1 裂缝证据分数（蚂蚁高值高分、相干低值高分、曲率取绝对值后高分），
  按层分别算稳健分位数，只保存小型统计合同，不产全区归一化体。
- 合理性：**方向正确**。与砂砾岩"不硬换算、方向统一"一致，且省掉了三份全区体。
- 问题：见 P0-2（v2/v3 合同分歧与混用）。

**3. 三界面逐道层位合同 + 延伸窗**（`common/horizon_contract`）
- 做法：160,801 道中 160,178 道有效（99.61%）；623 道层序倒转置 `SurfaceValid=0`；
  主窗 Top→Base + 延伸窗 Base→Base+50 ms。
- 合理性：**方向正确**，与砂砾岩"逐道层位合同是唯一时空基准"一致。
- 问题：合同制只落实到 Step6A/Step7A 一部分；Step6B/6C 仍在运行时做层位填补（P0-5）。

**4. 断层预处理 300 m 网格**（`common/fault_preprocessing_300m`）
- 做法：固定原点（658800/4231500，300 m），线段点按 `FaultNameRaw` 聚合成三维面，
  切 300 m 片，保留 `StickID`/`CellI/CellJ` 等追溯字段。
- 合理性：网格口径冻结是对的（避免不同区域任务各用一套原点）。
- 问题：**线段→三角面的几何方法没有在太古界验证**（砂砾岩那段代码只是参考实现），
  当前 demo 得到 361 个原始断层单元 / 3,495 三角面，其倾角、连续性、是否过拟合抽样线
  都没有 QC 记录（P1-5）。

### Step1–Step4（上游样本与模型）

**Step1 层位/时深/TVD 合同**：25 口常规井全部有本井时深与有效层序，20 口匹配到 GR/RD/RS。
合理性：把"合同齐备（eligible）"和"数值可用"分开，是对的。

**Step2 测井段底板**：33 口 → 17 口可用（10 常规 + 7 成像）；一井多段独立成文件、
重叠测次不合并；常规井逐行 X/Y 挂层位、成像井按 TVD 合同分层；直井用垂直回退。
- 合理性：**基本合理**。直井回退修得对；成像井按 TVD 分层（不用借用时深门控）也是对的。
- 问题：**一井多段只做到"输出多段文件"，没有做段间关系治理**（重叠/缺失/单行段），
  把问题全部推给了下游的 MD 回接（P0-4）。

**Step3 成像监督组**：8 组 / 6 井 / 28,751 行；重叠测次保留、组内可含近邻重复样本。
- 合理性：标签字段分层（`Density` / `HasFractureDensity` / `GT_POINT_FLAG`）是对的。
- 问题：**不同井的密度统计口径不统一**（FVDC / VDC01 / P10_SUM_C / P10_SUM_裂缝密度 /
  FVDC(P10)），当前决策是"暂时忽略口径差异，全部当作裂缝标签"（P0-3）；
  另外组内保留近邻重复样本会在训练时隐性加权。

**Step4 GR/RD/RS 两阶段预测**：45,830 行 / 17 井；裂缝点 4,553；
上部复合层 LOO AUC 0.43、风化壳 0.47/0.48。
- 合理性：两阶段 + 分层建模 + 只对完整三元曲线输出，沿用砂砾岩是对的。
- 问题：**指标低于随机，属于结构性瓶颈**（监督井 6 口、风化壳 2 口、标签口径不一），
  当前把它当基线是对的，但要写清"不可作地质解释"（P0-3）。
- 另：Step4 合并用 WellName+TVD 并查集 + **等权平均**多个重叠测次；诊断显示古斜19
  两个测次预测密度相关系数为负，说明"重叠测次不能简单平均"（P1-4）。

### Step5（虚拟井与统一样本）

- 做法：5×5 邻域（中心道保留，取消 40 m 硬门槛）；按层内相对位置重算虚拟 TIME；
  属性分数相似度 `exp(-|Δ|/scale)` 加权成连续性；另有一套**证据倍率**
  `IntensityMultiplier = clip(exp(beta × evidence), 0.25, 4.0)`；
  `DensityLabel = 源密度 × 倍率`（正样本）；`PointConfidence = 距离衰减 × 连续性`；
  `SampleWeight = 置信度/25`；缺主曲率或支持属性 < 2 直接剔除，其余软降权。
- 当前规模：统一样本 506,471 行（11 井）、虚拟候选 550,975、成像强监督 5,903、审计 22,848。
- 合理性：
  - "按层内相对位置重算 TIME"**完全正确**，这是砂砾岩虚拟井的核心，必须保留；
  - 双标签、权重/25、按源井分组，都对齐砂砾岩，合理；
  - 硬剔除标准收到"结构性不可用"（缺道/缺曲率/支持不足），比砂砾岩更宽松，方向合理。
- 问题：
  - **两套机制并存且未标定**：连续性已经不参与门控（`min_continuity=0`），只降权；
    而密度侧允许放大到 4 倍，`beta=0.25`、`clip=2`、倍率上下限没有标定记录（P1-1）。
    砂砾岩是"虚拟强度只能降低"，太古界改成可升可降，属于**方法层面的偏离**，需要论证。
  - `SourceSampleID` 用行号（`well::row{N}`），上游行序变化即失效（P2-1）。
  - 正式表实际 40 列，而方案要求 15 列紧凑合同，字段边界没有守住（P2-2）。
  - 配置仍引用 `attribute_contract_v2`（P0-2）。

### Step6A–D（三维证据体）

**Step6A**：两阶段模型 + 2 ms 密度体（160,801 道 × 600 样，2248–3446 ms）；
主窗均值 2.81、延伸窗 4.48；井上回放覆盖只有 5.8%（demo 盒内只有 406、405 两口井）。
- 合理性：两阶段、分层独立报告、按源井分组，正确。
- 问题：**延伸窗均值高于主窗却没有真值**，容易被误读为"下部更发育"（P1-3）；
  井上回放覆盖率过低，说明"全区预测"实际只覆盖 demo（P2-4）。

**Step6B 中尺度裂缝带**（v7）：10 ms/120 样；权重 AntTrack 0.70 / 低相干 0.15 / 曲率 0.15；
蚂蚁强响应不能被相干/曲率否决；632 个组件。
- 合理性：蚂蚁主导 + 几何过滤（倾角/线性/顺层）与砂砾岩一致，方向对。
- 问题：权重与砂砾岩（0.70/0.20/0.10）和早期文档（0.70/0.20/0.10）都不一致，
  且**没有权重标定依据**（P1-2）。

**Step6C 大尺度断层**（v3）：原始断层 361 单元 / 15 个断层名，原样复制；
属性自主断层 191 片，19 片被判为已知断层重复。
- 合理性：原始几何硬约束 + 自主断层分离，与砂砾岩一致，正确。
- 问题：自主断层完全由低相干驱动，而太古界相干体质量、断层线段解释密度都没做过
  "识别率/误报率"评估（P1-5）。

**Step6D 损伤带**：产出 `multiscale_damage_context_10ms.npz`。
- 问题：**这份输出没有任何下游消费**（Step7A 不读它，Step7B/C 也不用），
  砂砾岩"Step6D→Step7A 小尺度损伤带增强"这一环在太古界是断的（P0-6）。

### Step7A–D（多尺度 DFN）

**Step7A 小尺度片**：分层加权概率采样 + 3D 间距；方位用多井成像先验方向族 +
梯度局部方位；当前两个版本分别产出 22,665 片（attribute_v3_regen）和 22,019 片
（common_contract_v6_multiwell_gradient）。
- 合理性：概率采样替代全局阈值（避免"裂缝贴一个面"）是对的；成像方位先验也是对的。
- 问题：
  - **只采样主窗（`WindowCode=1`）**，延伸窗密度没有几何化，`all_patches_main_window` 强校验
    把这一限制固化了（P0-7）；
  - 片数从 OBN 时代的 249,928 骤降到 2.2 万，`occurrence_rate=0.015/0.01`、
    中心间距 20 m 这组参数**没有依据记录**（P1-6）。

**Step7B 中尺度片**（v7）：3,872 片，892 条带，上游组件覆盖 100%，脊线选点占比 0.6。
合理性：脊线优先 + 空间覆盖补点，与砂砾岩一致。

**Step7C 大尺度几何**（v3）：原始断层 SHA256 前后一致、3,495 个三角面完整携带；
191 片自主断层；`damage_zone_included_in_formal_dfn=False`。
合理性：**这是全链做得最规范的一环**（哈希校验 + 不截断 + 不片化），与砂砾岩一致。

**Step7D 融合**：26,728 片 = 22,665（小）+ 3,872（中）+ 191（大）。
合理性：三类片合并 + 原始断层作为第二几何组，正确。
- 问题：读的是 **7A attribute_v3_regen**（较旧），不是最新的 7A v6（P0-1）。

### Step8（井控校正）

- 做法：Step4 常规井点聚合成事件 + Step3 成像真值点（高优先）；只在小尺度可修改片内
  80 m / 30 ms 匹配；未命中则新增小尺度片；成像井周 150 m / 40 ms 软增强；
  改过的片重查 Top/Mid/Base 并做顶点裁剪；原始断层与中/大尺度不动。
- 当前结果：控制点 100（99 成像真值 + 1 个 Step4 事件），校正后 26,817 片。
- 合理性：控制源分级、只动小尺度、软增强不删片，全部与砂砾岩一致，正确。
- 问题：
  - **CONTROL 回接依赖 MD 且容差 0.011 m**，比 Step4 自身合并造成的网格偏移（0.042 m）
    还小，会静默丢点；同一模式在 Step5/7A/9 也存在（P0-4）；
  - **层位窗口门控用借用时深得到的 TIME**，把 405 的 316 个成像点剔掉 217 个（68%），
    与"借用时深不用于层位门控"的约定冲突（P0-8）；
  - 中/大尺度虽是"不动"，但校验方式是几何指纹，缺少"密度/面积"层面的守恒校验（P2-3）。

### Step9（剖面展示）

- 做法：沿 405 轨迹的弯曲 XZ/YZ 剖面；5 种背景（蚂蚁/相干/曲率/OBN 变密度/OBN 波形）
  × 2 个范围（overview + 井周 200 m）= 20 图；叠加 DFN 交线、层位线、成像点、井轨迹。
- 合理性：只读 Step8 统一 VTK、面-剖面交线（而不是散点），方向正确。
- 问题：
  - 图例/叠加层还没有"原始断层""成像裂缝样例""常规测井裂缝点位"这三类信息的完整表达
    （已知的下一步工作）；
  - OBN 背景与建模属性网格不同源，最近道采样可能错位或缺道，图上没有标注（P2-5）；
  - 剖面井 405 的成像点 TIME 是借用值，图上的"真实裂缝位置"本身不可信（P0-8）；
  - 同一 `md_merge_tolerance=0.011` 模式出现在这里（P0-4）。

---

## 第四部分：问题清单

### P0：影响正确性或可复现性，建议优先处理

| 编号 | 问题 | 证据 | 建议方向 |
|---|---|---|---|
| ~~P0-1~~ | **已解决（2026-09-15）**：正式链冻结为合同 v3 分支；并查明 7A `attribute_v3_regen` 与 `v6_multiwell_gradient` 配置逐键相同，"新方法"并未丢失 | 见第零部分 0.1；v2 分支已归档至 `_archive_20260915/A_contract_v2` | 已按较新分支定稿，无需重跑 |
| ~~P0-2~~ | **已解决（2026-09-15）**：统一到合同 v3（CurvatureMax 先取绝对值）；正式链 Step5/6A/6B/6C/7 均已是 v3，contract v2 已归档 | 两个 `attribute_normalization_contract.json` 逐字段对比；第零部分 0.1 | 后续新增步骤一律只引用 v3 合同 |
| P0-3 | **成像密度统计口径混用**：FVDC / VDC01 / P10_SUM_C / P10_SUM_裂缝密度 / FVDC(P10) 直接当同一回归标签 | `数据使用说明.md` §6.1"不能在未统一统计范围前混合"；Step1-4 交接文档决策 1"暂时忽略口径差异" | 逐井标注口径与临时性，或先只做单一统计范围的子集训练 |
| P0-4 | **四处重复且过紧的 MD 回接**：Step5 / Step7A / Step8 / Step9 各写一份 `merge_asof(tolerance=0.011)`；一井多段时 0.021 m 偏移即静默丢点 | Step8 实测：埕北310 全 9 点被丢；405 的 250/385 点只落在 0.005–0.010 之间擦边通过 | 抽成公共回接模块（段优先 + 容差放宽 + 未命中落审计），四处统一替换 |
| P0-5 | **运行时层位填补 vs 逐道合同**：`太古界逐道层位合同实现规划_20260902` 要求集中填补、下游只查表，但 **Step6A 预测** 与 **Step6B v7** 仍在运行时做 25 m 最近道层位填补（`predict_taigu_density_volume.py:314`、`build_step6b_medium_corridor_prior.py:123`）；Step6C 已直接读合同表、无此问题 | 两个脚本的 `horizon_fill_enabled/max_horizon_fill_distance_m`；6C 配置无该键 | 按规划把填补收进合同构建，下游删除运行时填补分支 |
| P0-6 | **损伤带链路断裂**：Step6D 产出 `multiscale_damage_context_10ms.npz` 但无任何下游消费 | 全仓 grep 无引用 | 要么接入 Step7A（按砂砾岩口径做邻域增强），要么明确标注本阶段不启用 |
| P0-7 | **延伸窗没有落地**：Step6A 预测了 Base 下 50 ms（均值 4.48），Step7A 只采样主窗并强校验 `all_patches_main_window`，Step8/9 也不区分 | Step7A 代码与 summary | 明确延伸窗是否进入 DFN；若进入，需在片表/剖面/验收里独立标记、低置信单列 |
| P0-8 | **借用时深被用作 TIME 域门控**：405 的 316 个成像真值点被层位窗口剔掉 217 个（68%），与"借用时深只作 TIME 标注、不作层位门控"冲突 | Step8 `well_control_outside_layer_audit.csv`；`数据使用说明.md` §2.4 | 成像井改用 TVD 合同门控，或提供 `layer_gate_domain=tvd` 开关；并在 summary 分列两套口径 |

### P1：口径/参数未标定，影响结论可信度

| 编号 | 问题 | 建议方向 |
|---|---|---|
| P1-1 | Step5 的"证据倍率"允许虚拟密度放大到 4 倍，`beta=0.25`、`clip=2`、上下限无标定记录；砂砾岩口径是"虚拟强度只能降低" | 用多井多层位做倍率分布与正样本率 QC，或先退回"≤1"的保守口径 |
| P1-2 | Step6B 权重 0.70/0.15/0.15 与砂砾岩 0.70/0.20/0.10 不一致，且无标定依据 | 记录权重来源，或做敏感性对比后再冻结 |
| P1-3 | 延伸窗密度均值（4.48）高于主窗（2.81）却无真值支撑 | 图上/表上强制标注低置信；做与风化壳一致性的对照 |
| P1-4 | Step4 对重叠测次做等权平均（古斜19 两测次相关系数为负） | 改为按测次质量/覆盖加权或择优选段，并保留诊断 |
| P1-5 | 断层线段→三维面→自主断层识别，缺少识别率/误报率评估 | 用已知断层做重合率/漏检率 QC，冻结参数 |
| P1-6 | Step7A 片数从 24.99 万降到 2.2 万，`occurrence_rate`、最小间距缺依据 | 记录参数来源，做分位数/密度一致性对比 |
| P1-7 | 风化壳强监督仅 2 井（313/816），却给出全区风化壳预测与 DFN | 明确风化壳结果置信度等级；优先补监督井 |

### P2：工程与展示

| 编号 | 问题 | 建议方向 |
|---|---|---|
| P2-1 | `SourceSampleID` 用行号（`well::row{N}`），上游行序变化即失效 | 改为内容键（井名+深度），与 Step8 的 ID 口径统一 |
| P2-2 | Step5 正式表 40 列，超出方案约定的 15 列紧凑合同 | 正式表与审计表分离 |
| P2-3 | Step8 对中/大尺度的"不动"只做几何指纹校验 | 增加密度质量/面积守恒校验 |
| P2-4 | Step6A"全区预测"实际只覆盖 5 km demo，井上回放仅 5.8% | 文案与 summary 明确范围；补齐矿区级版本再谈全区 |
| P2-5 | Step9 的 OBN 背景与属性主网格不同源，最近道采样可能错位 | 在图上/QC 标注 OBN 独立口径，或只用属性体做背景 |
| P2-6 | 没有统一 runner（旧 `run_taigu_step5_to_step9_v2.sh` 指向已删除的 Step9 脚本与配置），多尺度各步靠手动串 | 新建多尺度 runner，按版本校验每一步输入 |
| P2-7 | 配置命名混乱（6C 配置名仍是砂砾岩风格 `formal_candidate_cheye1_*`，各步 config 目录混有砂砾岩正式配置） | 统一命名与目录隔离，避免误用 |
| P2-8 | 文档与代码不一致：`太古界Step5最终方案_20260901` 描述"软门控/倍率"已部分实现，`数据使用说明` 的 6B 权重与 v7 配置不符 | 文档回写当前实现，或标注差异 |

---

## 第五部分：建议的改进顺序

1. **先统一口径，再谈调参**：先解决 P0-1（主链版本）与 P0-2（合同 v3），
   这两条决定了"现在这套结果到底是哪个口径"。
2. **再修回接与门控**：P0-4（公共回接模块）+ P0-8（成像井 TVD 门控），
   这两条直接决定井控是否用得上真实证据。
3. **补链路断点**：P0-5（层位合同集中填补）、P0-6（损伤带是否启用）、
   P0-7（延伸窗是否进 DFN）——每条都要么落地、要么显式关闭并写进 summary。
4. **标定类问题**（P1）建议在口径冻结后统一做一轮敏感性实验，用同一套留井验证比较。
5. **展示与工程**（P2）可以并行推进，其中 Step9 的图例/叠加层改造是当前明确的下一步。

---

## 第六部分：本次核验的证据索引

- 数据与口径：`太古界/数据使用说明.md`（§2 深度/坐标、§3.4 三属性、§3.5–3.6 断层、
  §6 成像标签、§7.2 井级可用性、§8 禁止规则）
- 上游：`太古界/太古界Step1-4交接文档_20260822.md`、`太古界/step4_fracture_prediction/太古界Step4目标层位与地震覆盖问题记录_20260901.md`
- 下游：`太古界/太古界Step5-9交接文档_20260822.md`、`太古界/step5_virtual_wells/太古界Step5最终方案_20260901.md`
- 公共层规划：`太古界/common/太古界逐道层位合同实现规划_20260902.md`、
  `太古界/common/attribute_sampling/太古界Common三属性合同修改方案_20260910.md`
- 中尺度：`太古界/step6b_medium_corridor/太古界中尺度裂缝流程方案_20260904.md`
- 配置与产物：各步 `configs/*.json` 与 `output/*/*summary*.json`（文中数字均来自这些文件）
- 代码：`step8_well_correction/correct_taigu_multiscale_dfn_with_well_controls.py`、
  `step5_virtual_wells/build_taigu_virtual_wells.py`、
  `step7a_small_scale_dfn/build_taigu_small_scale_dfn.py` 等
